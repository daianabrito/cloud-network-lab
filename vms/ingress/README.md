# Ingress VM

Role:
- Single public entrypoint
- Reverse proxy (Nginx)

Responsibilities:
- Accept HTTP traffic from outside
- Forward traffic to internal services (future)

Does NOT:
- Run application code
- Access database directly
